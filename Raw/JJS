-- =========================================
-- KNOR JJS - RETRO 2016 GUI (FULL SYSTEM + BLACK FLASH + KILL SOUND + DUYXD EXTENSIONS)
-- =========================================

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

-- Biến cấu hình GUI
local IsKeyVerified = false
local AutoAimEnabled = false
local CamlockEnabled = false
local AntiStunEnabled = false
local BlackFlashEnabled = false
local SlientRange = 25

-- Biến cấu hình Kill Sound
local KillSoundEnabled = false
local KillSoundId = "138556707979170"

local AutoAimConnection
local CamlockConnection
local AntiStunConnection

-- Biến cấu hình Black Flash
local CONFIG_BF = {
    BehindOffset = 6.5,
    FireDelay = 0.31,
    WarpDelay = 0.1,
}
local isCoolingBF = false

-- =========================================
-- CORE UTILS & BLACK FLASH HOOK
-- =========================================

local function GetCharacter()
    return LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
end

local function GetRoot()
    local Character = GetCharacter()
    return Character and Character:FindFirstChild("HumanoidRootPart")
end

local function GetHumanoid()
    local Character = GetCharacter()
    return Character and Character:FindFirstChildOfClass("Humanoid")
end

local function isAliveModel(model, exceptChar)
    if model == exceptChar then return false end
    local root = model:FindFirstChild("HumanoidRootPart")
    local humanoid = model:FindFirstChild("Humanoid")
    return root and humanoid and humanoid.Health > 0
end

-- Tìm Remote DivergentFist
local function getDivergentFistRemote()
    local ok, remote = pcall(function()
        return ReplicatedStorage
            :WaitForChild("Knit", 5)
            :WaitForChild("Knit", 5)
            :WaitForChild("Services", 5)
            :WaitForChild("DivergentFistService", 5)
            :WaitForChild("RE", 5)
            :WaitForChild("Activated", 5)
    end)
    return ok and remote or nil
end

local targetRemote = getDivergentFistRemote()

-- Tìm mục tiêu gần nhất cho Black Flash
local function findNearestTargetBF()
    local hrp = GetRoot()
    if not hrp then return nil end

    local myChar = LocalPlayer.Character
    local nearest = nil
    local bestDist = math.huge

    local function checkModel(model)
        if not isAliveModel(model, myChar) then return end
        local root = model:FindFirstChild("HumanoidRootPart")
        local dist = (hrp.Position - root.Position).Magnitude
        if dist < bestDist then
            bestDist = dist
            nearest = model
        end
    end

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            checkModel(player.Character)
        end
    end

    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("Model") then
            checkModel(obj)
        end
    end

    return nearest
end

-- Curved Dash Behind của Black Flash
local function curvedDashBehind(targetRoot, duration, customOffset)
    local char = LocalPlayer.Character
    if not char then return end

    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")

    if not hrp or not hum or not targetRoot then return end

    hum.AutoRotate = false

    local startTime = tick()
    local startPos = hrp.Position 
    local offsetDistance = customOffset or CONFIG_BF.BehindOffset

    local conn
    conn = RunService.RenderStepped:Connect(function()
        if not targetRoot or not targetRoot.Parent then
            hum.AutoRotate = true
            conn:Disconnect()
            return
        end

        local elapsed = tick() - startTime
        local alpha = math.clamp(elapsed / duration, 0, 1)

        if alpha >= 1 then
            hum.AutoRotate = true
            conn:Disconnect()
            return
        end

        local smoothAlpha = 1 - (1 - alpha)^3
        local enemyCF = targetRoot.CFrame
        local enemyPos = enemyCF.Position
        
        local behindPos = enemyPos - (enemyCF.LookVector * offsetDistance)
        local basePos = startPos:Lerp(behindPos, smoothAlpha)
        local curve = enemyCF.RightVector * math.sin(smoothAlpha * math.pi) * offsetDistance
        local finalPos = basePos + curve

        hrp.CFrame = CFrame.lookAt(finalPos, enemyPos)
    end)
end

-- Hook Metamethod cho Black Flash
if targetRemote then
    local oldNamecall
    oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
        if getnamecallmethod() ~= "FireServer" or self ~= targetRemote then
            return oldNamecall(self, ...)
        end

        if not BlackFlashEnabled or isCoolingBF then 
            return oldNamecall(self, ...)
        end

        isCoolingBF = true

        local args = {...}
        local result = oldNamecall(self, ...)

        task.delay(CONFIG_BF.FireDelay, function()
            pcall(function()
                targetRemote:FireServer(table.unpack(args))
            end)
            isCoolingBF = false
        end)

        task.delay(CONFIG_BF.WarpDelay, function()
            local target = findNearestTargetBF()
            if not target then return end
            local targetRoot = target:FindFirstChild("HumanoidRootPart")
            if not targetRoot then return end

            local myHrp = GetRoot()
            local chosenOffset = CONFIG_BF.BehindOffset

            if myHrp then
                local enemyLook = targetRoot.CFrame.LookVector
                local myLook = myHrp.CFrame.LookVector
                local dotProduct = enemyLook:Dot(myLook)
                
                if dotProduct > 0.5 then return end

                if math.abs(dotProduct) <= 0.5 then
                    chosenOffset = CONFIG_BF.BehindOffset * 0.5
                end
            end

            curvedDashBehind(targetRoot, CONFIG_BF.FireDelay, chosenOffset)
        end)

        return result
    end)
end

-- =========================================
-- LOGIC KHÁC & KILL SOUND
-- =========================================

local function StartAntiStun()
    if AntiStunConnection then AntiStunConnection:Disconnect() end
    AntiStunConnection = RunService.Stepped:Connect(function()
        if not AntiStunEnabled then return end
        local character = GetCharacter()
        local humanoid = GetHumanoid()
        if character and humanoid then
            pcall(function()
                if character:FindFirstChild("Stun") then
                    character.Stun:Destroy()
                end
                humanoid.PlatformStand = false
                humanoid.Sit = false
                if humanoid.WalkSpeed < 16 then
                    humanoid.WalkSpeed = 16
                end
            end)
        end
    end)
end

local function GetNearestTarget(maxRange)
    local Root = GetRoot()
    if not Root then return nil end

    local nearestTarget = nil
    local shortestDistance = maxRange

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local char = player.Character
            local rootPart = char and char:FindFirstChild("HumanoidRootPart")
            local humanoid = char and char:FindFirstChildOfClass("Humanoid")
            if rootPart and humanoid and humanoid.Health > 0 then
                local dist = (rootPart.Position - Root.Position).Magnitude
                if dist <= shortestDistance then
                    shortestDistance = dist
                    nearestTarget = rootPart
                end
            end
        end
    end

    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("Model") and obj:FindFirstChild("Humanoid") and obj:FindFirstChild("HumanoidRootPart") then
            local isPlayer = Players:GetPlayerFromCharacter(obj)
            if not isPlayer and obj.Humanoid.Health > 0 then
                local rootPart = obj.HumanoidRootPart
                local dist = (rootPart.Position - Root.Position).Magnitude
                if dist <= shortestDistance then
                    shortestDistance = dist
                    nearestTarget = rootPart
                end
            end
        end
    end

    return nearestTarget
end

local function GetPlayerNearCrosshair()
    local nearestPlayer = nil
    local shortestDistance = math.huge
    local mousePos = UserInputService:GetMouseLocation()

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local char = player.Character
            local head = char and char:FindFirstChild("Head")
            if head then
                local screenPoint, onScreen = Camera:WorldToViewportPoint(head.Position)
                if onScreen then
                    local dist = (Vector2.new(screenPoint.X, screenPoint.Y) - mousePos).Magnitude
                    if dist < shortestDistance then
                        shortestDistance = dist
                        nearestPlayer = head
                    end
                end
            end
        end
    end
    return nearestPlayer
end

local function StartAutoAim()
    if AutoAimConnection then AutoAimConnection:Disconnect() end
    AutoAimConnection = RunService.RenderStepped:Connect(function()
        if not AutoAimEnabled then return end
        local Root = GetRoot()
        if not Root then return end

        local target = GetNearestTarget(SlientRange)
        if target then
            local pos = Root.Position
            local targetPos = Vector3.new(target.Position.X, pos.Y, target.Position.Z)
            Root.CFrame = CFrame.lookAt(pos, targetPos)
        end
    end)
end

local function StartCamlock()
    if CamlockConnection then CamlockConnection:Disconnect() end
    CamlockConnection = RunService.RenderStepped:Connect(function()
        if not CamlockEnabled then return end
        local targetHead = GetPlayerNearCrosshair()
        local root = GetRoot()
        if targetHead and root then
            Camera.CFrame = CFrame.lookAt(Camera.CFrame.Position, targetHead.Position)
        end
    end)
end

-- =========================================
-- GUI CREATION (2016 RETRO - TAB MAIN SCROLLING)
-- =========================================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "KnorJJSGUI"
ScreenGui.ResetOnSpawn = false

local hui = (gethui and gethui()) or game:GetService("CoreGui")
if hui:FindFirstChild(ScreenGui.Name) then
    hui[ScreenGui.Name]:Destroy()
end

ScreenGui.Parent = hui

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 200, 0, 160)
MainFrame.Position = UDim2.new(0.5, -100, 0.4, -80)
MainFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
MainFrame.BorderSizePixel = 2
MainFrame.BorderColor3 = Color3.fromRGB(0, 132, 255)
MainFrame.Parent = ScreenGui

local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 20)
TitleBar.BackgroundColor3 = Color3.fromRGB(0, 102, 204)
TitleBar.BorderSizePixel = 0
TitleBar.Parent = MainFrame

local TitleText = Instance.new("TextLabel")
TitleText.Size = UDim2.new(1, -20, 1, 0)
TitleText.Position = UDim2.new(0, 5, 0, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = "Knor JJS"
TitleText.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleText.TextSize = 11
TitleText.Font = Enum.Font.SourceSansBold
TitleText.TextXAlignment = Enum.TextXAlignment.Left
TitleText.Parent = TitleBar

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 18, 0, 18)
CloseBtn.Position = UDim2.new(1, -19, 0, 1)
CloseBtn.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
CloseBtn.BorderColor3 = Color3.fromRGB(100, 0, 0)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 10
CloseBtn.Parent = TitleBar

local OpenBtn = Instance.new("TextButton")
OpenBtn.Size = UDim2.new(0, 35, 0, 35)
OpenBtn.Position = UDim2.new(0.02, 0, 0.4, 0)
OpenBtn.BackgroundColor3 = Color3.fromRGB(0, 102, 204)
OpenBtn.BorderColor3 = Color3.fromRGB(255, 255, 255)
OpenBtn.BorderSizePixel = 2
OpenBtn.Text = "JJS"
OpenBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
OpenBtn.Font = Enum.Font.SourceSansBold
OpenBtn.TextSize = 11
OpenBtn.Visible = false
OpenBtn.Parent = ScreenGui

local function EnableDrag(frame, dragHandle)
    local dragging, dragInput, dragStart, startPos
    dragHandle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    dragHandle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.Scale.X, startPos.Offset.X + delta.X, startPos.Scale.Y, startPos.Offset.Y + delta.Y)
        end
    end)
end

EnableDrag(MainFrame, TitleBar)
EnableDrag(OpenBtn, OpenBtn)

CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    OpenBtn.Visible = true
end)
OpenBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = true
    OpenBtn.Visible = false
end)

-- TAB SYSTEM
local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(1, 0, 0, 20)
TabBar.Position = UDim2.new(0, 0, 0, 20)
TabBar.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
TabBar.BorderSizePixel = 0
TabBar.Parent = MainFrame

local KeyTabBtn = Instance.new("TextButton")
KeyTabBtn.Size = UDim2.new(0.5, 0, 1, 0)
KeyTabBtn.BackgroundColor3 = Color3.fromRGB(55, 55, 55)
KeyTabBtn.BorderColor3 = Color3.fromRGB(20, 20, 20)
KeyTabBtn.Text = "Key System"
KeyTabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyTabBtn.Font = Enum.Font.SourceSansBold
KeyTabBtn.TextSize = 10
KeyTabBtn.Parent = TabBar

local MainTabBtn = Instance.new("TextButton")
MainTabBtn.Size = UDim2.new(0.5, 0, 1, 0)
MainTabBtn.Position = UDim2.new(0.5, 0, 0, 0)
MainTabBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
MainTabBtn.BorderColor3 = Color3.fromRGB(20, 20, 20)
MainTabBtn.Text = "Main (Locked)"
MainTabBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
MainTabBtn.Font = Enum.Font.SourceSansBold
MainTabBtn.TextSize = 10
MainTabBtn.Parent = TabBar

-- Tab Key System
local KeyContainer = Instance.new("Frame")
KeyContainer.Size = UDim2.new(1, 0, 1, -40)
KeyContainer.Position = UDim2.new(0, 0, 0, 40)
KeyContainer.BackgroundTransparency = 1
KeyContainer.Visible = true
KeyContainer.Parent = MainFrame

local KeyTextBox = Instance.new("TextBox")
KeyTextBox.Size = UDim2.new(0.8, 0, 0, 25)
KeyTextBox.Position = UDim2.new(0.1, 0, 0.15, 0)
KeyTextBox.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
KeyTextBox.BorderColor3 = Color3.fromRGB(0, 132, 255)
KeyTextBox.PlaceholderText = "ENTER KEY..."
KeyTextBox.Text = ""
KeyTextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyTextBox.Font = Enum.Font.SourceSans
KeyTextBox.TextSize = 11
KeyTextBox.Parent = KeyContainer

local CheckKeyBtn = Instance.new("TextButton")
CheckKeyBtn.Size = UDim2.new(0.8, 0, 0, 25)
CheckKeyBtn.Position = UDim2.new(0.1, 0, 0.45, 0)
CheckKeyBtn.BackgroundColor3 = Color3.fromRGB(0, 102, 204)
CheckKeyBtn.BorderColor3 = Color3.fromRGB(20, 20, 20)
CheckKeyBtn.Text = "Check Key"
CheckKeyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CheckKeyBtn.Font = Enum.Font.SourceSansBold
CheckKeyBtn.TextSize = 11
CheckKeyBtn.Parent = KeyContainer

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(0.9, 0, 0, 20)
StatusLabel.Position = UDim2.new(0.05, 0, 0.75, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = ""
StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
StatusLabel.Font = Enum.Font.SourceSansBold
StatusLabel.TextSize = 11
StatusLabel.Parent = KeyContainer

-- Tab Main (Scrolling Frame tự động CanvasSize)
local MainScrolling = Instance.new("ScrollingFrame")
MainScrolling.Size = UDim2.new(1, 0, 1, -40)
MainScrolling.Position = UDim2.new(0, 0, 0, 40)
MainScrolling.BackgroundTransparency = 1
MainScrolling.Visible = false
MainScrolling.AutomaticCanvasSize = Enum.AutomaticSize.Y
MainScrolling.CanvasSize = UDim2.new(0, 0, 0, 0)
MainScrolling.ScrollBarThickness = 4
MainScrolling.Parent = MainFrame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Parent = MainScrolling
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 6)
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local UIPadding = Instance.new("UIPadding")
UIPadding.Parent = MainScrolling
UIPadding.PaddingTop = UDim.new(0, 8)
UIPadding.PaddingBottom = UDim.new(0, 8)

-- =========================================
-- HÀM BỔ TRỢ ĐỂ DỄ DÀNG THÊM BUTTON SAU NÀY
-- =========================================

local currentOrder = 0

local function CreateMenuButton(name, onClick, isToggle)
    currentOrder = currentOrder + 1
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.88, 0, 0, 24)
    btn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
    btn.BorderColor3 = Color3.fromRGB(20, 20, 20)
    btn.Text = name
    btn.TextColor3 = isToggle and Color3.fromRGB(255, 100, 100) or Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 10
    btn.LayoutOrder = currentOrder
    btn.Parent = MainScrolling

    if onClick then
        btn.MouseButton1Click:Connect(function()
            onClick(btn)
        end)
    end
    return btn
end

-- =========================================
-- TẠO CÁC NÚT TÍNH NĂNG TRÊN MAIN TAB
-- =========================================

-- 1. Toggle: Black Flash
CreateMenuButton("BlackFlash : OFF", function(btn)
    BlackFlashEnabled = not BlackFlashEnabled
    if BlackFlashEnabled then
        btn.Text = "BlackFlash : ON"
        btn.TextColor3 = Color3.fromRGB(100, 255, 100)
        btn.BackgroundColor3 = Color3.fromRGB(50, 90, 50)
    else
        btn.Text = "BlackFlash : OFF"
        btn.TextColor3 = Color3.fromRGB(255, 100, 100)
        btn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
    end
end, true)

-- 2. Toggle: Anti Stun
CreateMenuButton("Anti Stun: OFF", function(btn)
    AntiStunEnabled = not AntiStunEnabled
    if AntiStunEnabled then
        btn.Text = "Anti Stun: ON"
        btn.TextColor3 = Color3.fromRGB(100, 255, 100)
        btn.BackgroundColor3 = Color3.fromRGB(50, 90, 50)
        StartAntiStun()
    else
        btn.Text = "Anti Stun: OFF"
        btn.TextColor3 = Color3.fromRGB(255, 100, 100)
        btn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
        if AntiStunConnection then AntiStunConnection:Disconnect() end
    end
end, true)

-- 3. Toggle: Auto Aim Lock
CreateMenuButton("Auto Aim Lock: OFF", function(btn)
    AutoAimEnabled = not AutoAimEnabled
    if AutoAimEnabled then
        btn.Text = "Auto Aim Lock: ON"
        btn.TextColor3 = Color3.fromRGB(100, 255, 100)
        btn.BackgroundColor3 = Color3.fromRGB(50, 90, 50)
        StartAutoAim()
    else
        btn.Text = "Auto Aim Lock: OFF"
        btn.TextColor3 = Color3.fromRGB(255, 100, 100)
        btn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
        if AutoAimConnection then AutoAimConnection:Disconnect() end
    end
end, true)

-- 4. Toggle: Camlock (beta)
CreateMenuButton("Camlock (beta): OFF", function(btn)
    CamlockEnabled = not CamlockEnabled
    if CamlockEnabled then
        btn.Text = "Camlock (beta): ON"
        btn.TextColor3 = Color3.fromRGB(100, 255, 100)
        btn.BackgroundColor3 = Color3.fromRGB(50, 90, 50)
        StartCamlock()
    else
        btn.Text = "Camlock (beta): OFF"
        btn.TextColor3 = Color3.fromRGB(255, 100, 100)
        btn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
        if CamlockConnection then CamlockConnection:Disconnect() end
    end
end, true)

-- 5. Toggle: Kill Sound
CreateMenuButton("Kill Sound: OFF", function(btn)
    KillSoundEnabled = not KillSoundEnabled
    if KillSoundEnabled then
        btn.Text = "Kill Sound: ON"
        btn.TextColor3 = Color3.fromRGB(100, 255, 100)
        btn.BackgroundColor3 = Color3.fromRGB(50, 90, 50)
    else
        btn.Text = "Kill Sound: OFF"
        btn.TextColor3 = Color3.fromRGB(255, 100, 100)
        btn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
    end
end, true)

-- 6. Textbox: Kill Sound ID
currentOrder = currentOrder + 1
local KillSoundBox = Instance.new("TextBox")
KillSoundBox.Size = UDim2.new(0.88, 0, 0, 24)
KillSoundBox.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
KillSoundBox.BorderColor3 = Color3.fromRGB(20, 20, 20)
KillSoundBox.TextColor3 = Color3.fromRGB(255, 255, 255)
KillSoundBox.PlaceholderColor3 = Color3.fromRGB(150, 150, 150)
KillSoundBox.PlaceholderText = "Sound ID"
KillSoundBox.Text = KillSoundId
KillSoundBox.TextSize = 10
KillSoundBox.Font = Enum.Font.SourceSans
KillSoundBox.ClearTextOnFocus = false
KillSoundBox.LayoutOrder = currentOrder
KillSoundBox.Parent = MainScrolling

local KillSoundObject = Instance.new("Sound")
KillSoundObject.Name = "KillSoundAudio"
KillSoundObject.Volume = 1
KillSoundObject.SoundId = "rbxassetid://" .. KillSoundId
KillSoundObject.Parent = ScreenGui

KillSoundBox.FocusLost:Connect(function()
    local ID = KillSoundBox.Text:match("%d+")
    if ID then
        KillSoundId = ID
        KillSoundObject.SoundId = "rbxassetid://" .. KillSoundId
    else
        KillSoundBox.Text = KillSoundId
    end
end)

local function PlayKillSound()
    if not KillSoundEnabled then return end
    KillSoundObject.SoundId = "rbxassetid://" .. KillSoundId
    KillSoundObject.TimePosition = 0
    KillSoundObject:Play()
end

-- 7. Slider: Slient Range (10 - 100)
currentOrder = currentOrder + 1
local SliderContainer = Instance.new("Frame")
SliderContainer.Size = UDim2.new(0.88, 0, 0, 24)
SliderContainer.BackgroundTransparency = 1
SliderContainer.LayoutOrder = currentOrder
SliderContainer.Parent = MainScrolling

local RangeLabel = Instance.new("TextLabel")
RangeLabel.Size = UDim2.new(1, 0, 0, 12)
RangeLabel.Position = UDim2.new(0, 0, 0, 0)
RangeLabel.BackgroundTransparency = 1
RangeLabel.Text = "Slient Range: 25 studs"
RangeLabel.TextColor3 = Color3.fromRGB(230, 230, 230)
RangeLabel.Font = Enum.Font.SourceSans
RangeLabel.TextSize = 10
RangeLabel.Parent = SliderContainer

local SliderFrame = Instance.new("Frame")
SliderFrame.Size = UDim2.new(1, 0, 0, 8)
SliderFrame.Position = UDim2.new(0, 0, 0, 14)
SliderFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
SliderFrame.BorderColor3 = Color3.fromRGB(10, 10, 10)
SliderFrame.Parent = SliderContainer

local SliderFill = Instance.new("Frame")
SliderFill.Size = UDim2.new((SlientRange - 10) / (100 - 10), 0, 1, 0)
SliderFill.BackgroundColor3 = Color3.fromRGB(0, 132, 255)
SliderFill.BorderSizePixel = 0
SliderFill.Parent = SliderFrame

local Sliding = false
local function UpdateRangeSlider(input)
    local pos = math.clamp((input.Position.X - SliderFrame.AbsolutePosition.X) / SliderFrame.AbsoluteSize.X, 0, 1)
    SlientRange = math.floor(10 + (pos * (100 - 10)))
    SliderFill.Size = UDim2.new(pos, 0, 1, 0)
    RangeLabel.Text = "Slient Range: " .. tostring(SlientRange) .. " studs"
end

SliderFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        Sliding = true
        UpdateRangeSlider(input)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        Sliding = false
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if Sliding and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        UpdateRangeSlider(input)
    end
end)

-- 8. Button: DuyXD Sweep all (Link Pastefy) ⚡
CreateMenuButton("DuyXD Sweep all", function(btn)
    pcall(function()
        loadstring(game:HttpGet("https://pastefy.app/wIg0pRUl/raw"))()
    end)
end, false)

-- =========================================
-- CHUYỂN TAB & KEY SYSTEM
-- =========================================

KeyTabBtn.MouseButton1Click:Connect(function()
    KeyContainer.Visible = true
    MainScrolling.Visible = false
    KeyTabBtn.BackgroundColor3 = Color3.fromRGB(55, 55, 55)
    MainTabBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
end)

MainTabBtn.MouseButton1Click:Connect(function()
    if IsKeyVerified then
        KeyContainer.Visible = false
        MainScrolling.Visible = true
        MainTabBtn.BackgroundColor3 = Color3.fromRGB(55, 55, 55)
        KeyTabBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    else
        StatusLabel.Text = "Chưa nhập Key hợp lệ!"
    end
end)

CheckKeyBtn.MouseButton1Click:Connect(function()
    if KeyTextBox.Text == "Knor_KeyLess" then
        IsKeyVerified = true
        StatusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
        StatusLabel.Text = "Đúng Key! Đã mở tab Main."
        MainTabBtn.Text = "Main"
        MainTabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    else
        StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
        StatusLabel.Text = "Key sai!"
    end
end)

-- Find kills stat & Monitor
local function FindKills()
    local leaderstats = LocalPlayer:FindFirstChild("leaderstats")
    if not leaderstats then return nil end

    local possibleNames = {"Kills", "Kill", "KOs", "KO", "Eliminations", "Elims"}
    for _, Name in ipairs(possibleNames) do
        local Stat = leaderstats:FindFirstChild(Name)
        if Stat and (Stat:IsA("IntValue") or Stat:IsA("NumberValue")) then
            return Stat
        end
    end
    return nil
end

task.spawn(function()
    local KillStat
    repeat
        task.wait(1)
        KillStat = FindKills()
    until KillStat

    local LastKills = KillStat.Value
    KillStat:GetPropertyChangedSignal("Value"):Connect(function()
        local NewKills = KillStat.Value
        if NewKills > LastKills then
            for _ = LastKills + 1, NewKills do
                PlayKillSound()
            end
        end
        LastKills = NewKills
    end)
end)
